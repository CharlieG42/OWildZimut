name: o_wild_zimut
description: Outil de creation de cartes de Course d'Orientation avec gestion de calques
publish_to: 'none'
version: 0.0.001+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/
