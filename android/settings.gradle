pluginManagement {
    def flutterSdkPath = System.getenv('FLUTTER_ROOT') ?: '.flutter'
    includeBuild("${flutterSdkPath}/packages/flutter_tools/gradle")
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'o_wild_zimut'
include ':app'
