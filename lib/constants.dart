import 'package:flutter/material.dart';
/// Constantes globales de l'application OWildZimut
/// 
/// Ce fichier contient toutes les constantes partagées dans l'application.
/// Modifier ces valeurs pour mettre à jour les paramètres globaux.


class AppConstants {
  /// Version actuelle de l'application
  /// Modifier cette valeur unique pour mettre à jour la version partout dans l'application
  static const String version = '0.0.007';
  static const Offset defaultPanOffset = Offset(200,200);
}
